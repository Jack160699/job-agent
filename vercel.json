{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron?mode=drain",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/cron?mode=schedule",
      "schedule": "0 6 * * *"
    }
  ]
}
